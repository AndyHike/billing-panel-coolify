# Coolify Admin Panel

Надбудова для Coolify з адмін-панеллю для управління клієнтами та проектами.

## Можливості

- Авторизація через email + пароль (bcrypt)
- Управління клієнтами (створення, редагування, перегляд)
- Присвоєння проектів до клієнтів з тарифікацією
- Синхронізація проектів з Coolify через API
- Запуск/зупинка всіх ресурсів проекту
- Автоматична пауза проектів при закінченні терміну
- Docker deployment ready для Coolify

## Технології

- **Next.js 16** (App Router)
- **PostgreSQL** (через `pg` - прямі SQL запити)
- **Coolify API** (управління проектами)
- **bcryptjs** (хешування паролів)
- **jose** (JWT авторизація)
- **Tailwind CSS v4** + shadcn/ui

## Швидкий старт

### 1. Підготовка БД

Запустіть SQL скрипт для створення всіх таблиць:

\`\`\`bash
psql "YOUR_DATABASE_URL" -f scripts/create-tables.sql
\`\`\`

Це створить:
- Таблиці: `users`, `clients`, `projects`, `client_projects`, `quotas`
- Індекси для оптимізації запитів
- Тригери для автоматичного оновлення `updated_at`
- Тестового адміна: `admin@coolify.local` / `admin123`

### 2. Налаштування змінних середовища

Створіть файл `.env` або налаштуйте змінні в Coolify:

\`\`\`env
DATABASE_URL="postgres://postgres:fordev@q4o0kwc8880s0o88oc4008go:5432/postgres?sslmode=require"
JWT_SECRET="your-secret-jwt-key"
COOLIFY_API_URL="http://localhost:8000"
COOLIFY_API_TOKEN="your-coolify-api-token"
CRON_SECRET="your-cron-secret"
\`\`\`

### 3. Локальна розробка

\`\`\`bash
npm install
npm run dev
\`\`\`

Відкрийте http://localhost:3000

**Дані для входу за замовчуванням:**
- Email: `admin@coolify.local`
- Пароль: `admin123`

### 4. Deployment в Coolify

1. **Створіть проект в Coolify**
   - Source: Git Repository
   - Dockerfile: використовуйте наявний Dockerfile

2. **Налаштуйте змінні середовища:**
   ```
   DATABASE_URL=postgres://...
   JWT_SECRET=your-secret-key
   COOLIFY_API_URL=http://host.docker.internal:8000
   COOLIFY_API_TOKEN=your-token
   CRON_SECRET=your-cron-secret
   ```
   
   **ВАЖЛИВО - COOLIFY_API_URL:**
   - Якщо деплоїте В САМОМУ Coolify, використовуйте:
     - `http://host.docker.internal:8000` (для доступу до хоста з контейнера)
     - АБО `http://172.17.0.1:8000` (Docker bridge IP)
     - АБО внутрішню адресу вашого Coolify сервісу
   - НЕ використовуйте зовнішній IP (`http://31.97.129.75:8000`) - Docker контейнер не зможе підключитися!

3. **Build Command:**
   ```
   npm run build
   ```

4. **Start Command:**
   ```
   npm start
   ```

5. **Deploy!**

## Структура проекту

\`\`\`
├── app/
│   ├── api/                    # API routes
│   │   ├── auth/              # Авторизація (login, logout)
│   │   ├── clients/           # CRUD клієнтів
│   │   ├── client-projects/   # Управління проектами клієнтів
│   │   ├── projects/          # Синхронізація з Coolify
│   │   └── cron/              # Автоматична перевірка підписок
│   ├── dashboard/             # Адмін панель
│   │   ├── clients/           # Сторінки клієнтів
│   │   └── projects/          # Сторінки проектів
│   └── login/                 # Сторінка входу
├── components/
│   ├── dashboard/             # Компоненти панелі
│   └── ui/                    # shadcn/ui компоненти
├── lib/
│   ├── db.ts                  # PostgreSQL connection (pg)
│   ├── auth.ts                # JWT авторизація + bcrypt
│   └── coolify.ts             # Coolify API клієнт
├── scripts/
│   └── create-tables.sql      # SQL скрипт створення таблиць
├── Dockerfile                 # Docker для Coolify
├── docker-compose.yml         # Compose для Coolify
└── middleware.ts              # Захист маршрутів

\`\`\`

## Робота з проектами

### Синхронізація з Coolify

1. Перейдіть в "Проекти" → "Синхронізувати проекти"
2. Натисніть кнопку синхронізації
3. Всі проекти з Coolify будуть імпортовані в БД

### Додавання проекту до клієнта

1. Відкрийте клієнта
2. Натисніть "Додати проект"
3. Оберіть проект зі списку
4. Вкажіть дату закінчення підписки
5. Проект буде активований

### Управління проектами

- **▶️ Start** - запускає всі ресурси проекту через Coolify API
- **⏸️ Pause** - зупиняє всі ресурси проекту
- **🗑️ Delete** - видаляє проект від клієнта (сам проект в Coolify залишається)

## Автоматична пауза

Для автоматичної паузи проектів при закінченні терміну, налаштуйте cron job:

**Coolify Cron Job:**
- URL: `https://your-domain.com/api/cron/check-subscriptions`
- Method: GET
- Header: `Authorization: Bearer YOUR_CRON_SECRET`
- Schedule: `0 */1 * * *` (кожну годину)

## Database Schema

### users
- Адміністратори системи
- Авторизація через email/password (bcrypt)

### clients
- Клієнти
- Основна інформація (ім'я, email, телефон, нотатки)

### projects
- Проекти з Coolify
- `coolify_uuid` - UUID з Coolify API
- `name`, `description`, `status`

### client_projects
- Зв'язок клієнтів і проектів (many-to-many)
- Тарифікація (`start_date`, `end_date`)
- Статус (`status`: 'active', 'paused')

### quotas
- Квоти для проектів (згідно з вимогами)
- usedBytes, limitBytes

## API Endpoints

### Авторизація
- `POST /api/auth/login` - Вхід
- `POST /api/auth/logout` - Вихід

### Клієнти
- `GET /api/clients` - Список клієнтів
- `POST /api/clients` - Створити клієнта
- `POST /api/clients/[id]/projects` - Додати проект до клієнта

### Проекти
- `POST /api/projects/sync` - Синхронізація з Coolify

### Управління
- `POST /api/client-projects/[id]/start` - Запустити проект
- `POST /api/client-projects/[id]/stop` - Зупинити проект
- `DELETE /api/client-projects/[id]` - Видалити зв'язок

### Cron
- `GET /api/cron/check-subscriptions` - Перевірка та пауза просрочених проектів

## Безпека

- Паролі хешовані через bcrypt (10 rounds)
- JWT токени для сесій (httpOnly cookies)
- Middleware захист для всіх маршрутів dashboard
- API endpoints захищені авторизацією
- Cron endpoints захищені секретним ключем
- SQL injection захист через параметризовані запити (`$1`, `$2`)
- HTTPS через Traefik в Coolify

## База даних

Проект використовує **прямі SQL запити** без ORM через бібліотеку `pg` (node-postgres).

### Підключення
- Через змінну `DATABASE_URL`
- Підтримка SSL (`sslmode=require`)
- Connection pooling (max 20 з'єднань)

### Міграції
Всі зміни структури БД виконуються через SQL скрипти:
```bash
psql "$DATABASE_URL" -f scripts/create-tables.sql
```

### Параметризовані запити
Всі запити використовують параметризацію для захисту від SQL injection:
```typescript
await query('SELECT * FROM users WHERE email = $1', [email])
```

## Troubleshooting

### Помилка підключення до БД
- Перевірте `DATABASE_URL` в змінних середовища
- Переконайтесь що БД доступна з контейнера
- Для зовнішнього доступу використовуйте публічний URL з SSL

### Проекти не синхронізуються (Connect Timeout Error)
**Причина:** Docker контейнер не може підключитися до Coolify API

**Рішення:**
1. Якщо деплоїте В САМОМУ Coolify, змініть `COOLIFY_API_URL`:
   - Спробуйте `http://host.docker.internal:8000`
   - Або `http://172.17.0.1:8000`
   - НЕ використовуйте зовнішній IP!

2. Перевірте токен:
   - `COOLIFY_API_TOKEN` повинен бути валідним
   - Створіть в Coolify: Settings → API → Create Token
   
3. Тест підключення:
   ```bash
   # З контейнера спробуйте:
   curl -H "Authorization: YOUR_TOKEN" http://host.docker.internal:8000/api/v1/projects
   ```

4. Перевірте логи для діагностики:
   - Дивіться в логи додатку для деталей помилки
   - Шукайте "Coolify API request" та "Connect Timeout"

### Не працює автоматична пауза
- Налаштуйте cron job з правильним `CRON_SECRET`
- Перевірте логи endpoint `/api/cron/check-subscriptions`

## Підтримка

Створюйте issues в репозиторії проекту.

## Ліцензія

MIT
